package com.example.htmlFilePath;

public class FileUploadUtil {
    
    
    
    
//  public static int saveFile(String fileName, MultipartFile multipartFile)
//  throws IOException {
//
//
//String currentDirectory = System.getProperty("user.dir");
//Path uploadPath = Paths.get(currentDirectory);
//
//String folderName = "HtmlDownloads";
//
//// Create the folder if it doesn't exist
//Path folderPath = uploadPath.resolve(folderName);
//
//if (!Files.exists(folderPath)) {
//  Files.createDirectories(folderPath);
//}
//
//Random random = new Random();
//
//// Generate a random numeric integer with 6 digits
//int randomInteger = random.nextInt(900000) + 100000;
//try (InputStream inputStream = multipartFile.getInputStream()) {
//  Path filePath = folderPath.resolve(fileName);
//  Files.copy(inputStream, filePath, StandardCopyOption.REPLACE_EXISTING);
//} catch (IOException ioe) {
//  throw new IOException("Could not save file: " + fileName, ioe);
//}
//
//return randomInteger;
//}
//


}
